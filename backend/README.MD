# Fridge helper - backend

Directly connect to db: `flyctl postgres connect -a <app_name-db>`

Revert latest migration & seed: `npm run migration:down`

Revert all migrations & seeds (clear postgres): `npm run migrations:down`
